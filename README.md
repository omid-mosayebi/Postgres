# Postgres
it has 2 files that are usefull 

1 - in restoring file that script first a database will be created than your backup will be restored and finally could be verified.

2- in Postgres backup automatically: there is some variables that have to change such as user and password

